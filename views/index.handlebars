<h1>boorgors</h1>

<h2>borgers that been ate</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>byurgyurs that aint ate</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a birgir</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">bergare Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">been ate?</label><br>
    <input type="radio" name="devoured" value="1" checked> aint been ate<br>
    <input type="radio" name="devoured" value="0"> been ate!
  </div>

  <button type="submit">Add brgr</button>
</form>
